local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Hunty Zombie",
    SubTitle = "By Hollow Moon Hub",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})
local Tabs = {
    Lobby = Window:AddTab({ Title = "Lobby", Icon = "armchair" }),
    Farm = Window:AddTab({ Title = "Farm", Icon = "monitor" }),
    Combat = Window:AddTab({ Title = "Combat", Icon = "sword" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

local Options = Fluent.Options

local FarmTab = Tabs.Farm

local AutoClick = false

FarmTab:AddToggle("AutoLeftClick", {
    Title = "Auto Left Mouse Click",
    Description = "Rapidly clicks left mouse button while enabled",
    Default = false,
    Callback = function(state)
        AutoClick = state
        
        if state then
            Fluent:Notify({
                Title = "Auto Click",
                Content = "Left Mouse Auto-Click ENABLED",
                Duration = 3
            })
        else
            Fluent:Notify({
                Title = "Auto Click",
                Content = "Left Mouse Auto-Click DISABLED",
                Duration = 3
            })
        end
    end
})

-- Auto Click Logic
game:GetService("RunService").RenderStepped:Connect(function()
    if AutoClick then
        local mouse = game.Players.LocalPlayer:GetMouse()
        mouse.Button1Down:Fire()  -- Fires left click
    end
end)

SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({})
InterfaceManager:SetFolder("AnimeFinalQuest")
SaveManager:SetFolder("AnimeFinalQuest/specific-game")
InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)

SaveManager:LoadAutoloadConfig()
